
/**
 * parses a given command and executes the according effects in the given world
 * @param {Number} command
 * @param {Number} world
 * @return {Array} array of strings containing messages
 */
export let parse = (command, world) => {

    // TODO

    return [ 'unknown command' ];
};
