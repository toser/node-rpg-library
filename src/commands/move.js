
export const cmdRegExp = /^(move|move to|m) *(east|west|north|south|left|right|up|down){0,1}$/;

export const run = (player, command, world) => {
    return [ `${player} moves (like Jagger?) *TODO*` ];
};
